service: () -> {
    createEscrow: (principal, principal, principal, nat64, text, nat64) -> (text);
    greet: (text) -> (text) query;
    releaseEscrow: (text) -> (variant {Ok:text; Err:text}) query;
}
